server:
  port: 8080

spring:
  application:
    name: e-fyp
  profiles:
    active: @spring.profiles.active@

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    root: info
    web: info